spring.datasource.url=jdbc:postgresql://${DATABASE_URL}:${DATABASE_PORT}/postgres
spring.datasource.username=postgres
spring.datasource.password=123

rabbitmq.create=${RABBITMQ_QUEUE_CREATE}
rabbitmq.update=${RABBITMQ_QUEUE_UPDATE}
rabbitmq.delete=${RABBITMQ_QUEUE_DELETE}
rabbitmq.patch.consumer=${RABBITMQ_QUEUE_PATCH_CONSUMER}

spring.rabbitmq.host=${RABBITMQ_URL}
spring.rabbitmq.port=${RABBITMQ_PORT}
spring.rabbitmq.username=${RABBITMQ_USERNAME}
spring.rabbitmq.password=${RABBITMQ_PASSWORD}

spring.database.driverClassName=org.postgresql.Driver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQL81Dialect
